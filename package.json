{
  "name": "gansohosting",
  "description": "React Gatsby Satic Site Generator",
  "version": "1.1",
  "private": true,
  "author": "Rudy Bekker",
  "devDependencies": {
    "cross-env": "^7.0.2",
    "husky": "^7.0.2",
    "lint-staged": "^11.1.2",
    "prettier": "^2.1.2",
    "rimraf": "^3.0.2"
  },
  "workspaces": [
    "packages/landing-gatsby"
  ],
  "scripts": {
    "clean": "yarn workspaces run rimraf \"{.next,node_modules,.cache,out,dist}\" && rimraf node_modules",
    "gatsby-dev": "yarn workspace @gansosite/gatsby run dev",
    "gatsby-build": "yarn workspace @gansosite/gatsby run build",
    "gatsby-serve": "yarn workspace @gansosite/gatsby run start"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{js,md,css}": [
      "prettier --trailing-comma es5 --single-quote --write"
    ]
  },
  "dependencies": {}
}
